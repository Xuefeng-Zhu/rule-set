# rule-set

A simple node module to manage dependence

## Usage

```
const {
  makeRelationshipSet,
  dependsOn,
  areExclusive,
  checkRelationships,
  toggle,
} = require('rule-set');
```

## Test

```
npm run test
```